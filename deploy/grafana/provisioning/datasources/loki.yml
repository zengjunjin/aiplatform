# Task 87: Grafana 自动 provisioning — Loki 数据源
# 配合 deploy/grafana/provisioning/datasources.yml（Prometheus）使用
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: false
    editable: false
    jsonData:
      maxLines: 1000
